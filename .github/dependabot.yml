version: 2

updates:
  - package-ecosystem: bundler
    directory: /
    schedule:
      interval: daily

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: daily

  - package-ecosystem: npm
    directory: /web
    schedule:
      interval: daily
    groups:
      # @glint/template and @glint/ember-tsc ship on separate release lines
      # and are incompatible unless bumped together, so update them in a
      # single PR.
      glint:
        patterns:
          - '@glint/*'

      # ember-intl and the @ember-intl/* packages divide the work of loading
      # translations between them, so a major bump of one without the others
      # leaves the app unable to read its own translation files.
      ember-intl:
        patterns:
          - 'ember-intl'
          - '@ember-intl/*'
    ignore:
      # Babel 8 requires the whole @babel/* graph to move together, but the
      # Ember build toolchain (ember-cli-babel, broccoli-babel-transpiler)
      # still pins @babel/core ^7. Until that ecosystem supports Babel 8,
      # major bumps of individual @babel/* packages only break the build.
      - dependency-name: '@babel/*'
        update-types:
          - version-update:semver-major
